# Local development, matching docker-compose.yml.
#
# Port 5433, not the Postgres default: a dev machine usually already has
# something on 5432. Change both here and POSTGRES_PORT if you move it.
DATABASE_URL="postgresql://jobsearch:jobsearch@localhost:5433/jobsearch?schema=public"
